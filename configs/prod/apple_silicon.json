{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "results/apple_silicon_training",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 50,
  "max_steps_per_epoch": 500,
  "batch_size": 64,
  "learning_rate_policy": 3e-4,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "device": "mps",
  "save_every": 5,
  "eval_every": 2,
  "patience": 10,
  "log_freq": 50,
  "eval_freq": 250,
  "checkpoint_freq": 500,
  "gradient_accumulation_steps": 16,
  "micro_batch_size": 4,
  "comment": "Apple Silicon memory-optimized config - reduced memory footprint while maintaining effective batch_size=64 through 16x4 gradient accumulation"
}