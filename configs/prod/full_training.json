{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "results/full_training",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 100,
  "max_steps_per_epoch": 1000,
  "batch_size": 32,
  "learning_rate_policy": 3e-4,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "device": "auto",
  "save_every": 5,
  "eval_every": 2,
  "patience": 20,
  "log_freq": 100,
  "eval_freq": 500,
  "checkpoint_freq": 1000,
  "gradient_accumulation_steps": 8,
  "micro_batch_size": 4,
  "resume": true,
  "comment": "Full 100-epoch production training with automatic resume capability - effective batch_size=32 with 8x4 gradient accumulation"
}