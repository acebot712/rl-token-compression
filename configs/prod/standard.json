{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "results/standard_training",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 50,
  "max_steps_per_epoch": 500,
  "batch_size": 32,
  "learning_rate_policy": 3e-4,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "device": "auto",
  "save_every": 10,
  "eval_every": 5,
  "patience": 15,
  "log_freq": 100,
  "eval_freq": 500,
  "checkpoint_freq": 1000,
  "gradient_accumulation_steps": 8,
  "micro_batch_size": 4,
  "comment": "Standard production config with memory efficiency - works on 16GB+ systems, effective batch_size=32 with 8x4 gradient accumulation"
}