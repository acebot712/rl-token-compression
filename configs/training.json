{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "outputs/training",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 100,
  "max_steps_per_epoch": 1000,
  "batch_size": 64,
  "learning_rate_policy": 5e-4,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "importance_weighting": false,
  "temperature_schedule": "linear",
  "device": "auto",
  "save_every": 5,
  "eval_every": 5,
  "patience": 20,
  "log_freq": 50,
  "eval_freq": 500,
  "checkpoint_freq": 500,
  "gradient_accumulation_steps": 8,
  "micro_batch_size": 8,
  "resume": true,
  "comment": "Conservative production config - device=auto, moderate batch_size=64 works across hardware, use training_mps.json or training_cuda.json for hardware-specific optimization"
}