{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "outputs/training_cuda",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 100,
  "max_steps_per_epoch": 1000,
  "batch_size": 256,
  "learning_rate_policy": 1e-3,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "importance_weighting": false,
  "temperature_schedule": "linear",
  "device": "cuda",
  "save_every": 5,
  "eval_every": 5,
  "patience": 20,
  "log_freq": 50,
  "eval_freq": 500,
  "checkpoint_freq": 500,
  "gradient_accumulation_steps": 16,
  "micro_batch_size": 16,
  "resume": true,
  "comment": "CUDA-optimized config for NVIDIA GPUs - Higher batch sizes and learning rates for GPU memory capacity, stable RL gradients with information-theoretic rewards"
}