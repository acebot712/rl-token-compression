{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "outputs/training_mps",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 100,
  "max_steps_per_epoch": 1000,
  "batch_size": 16,
  "learning_rate_policy": 3e-4,
  "learning_rate_reconstructor": 1e-4,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "device": "mps",
  "save_every": 5,
  "eval_every": 5,
  "patience": 20,
  "log_freq": 50,
  "eval_freq": 500,
  "checkpoint_freq": 500,
  "gradient_accumulation_steps": 8,
  "micro_batch_size": 2,
  "resume": true,
  "comment": "MPS-optimized config for Apple Silicon - Proven working settings: micro_batch_size=2 with 8x accumulation for effective batch_size=16, memory-efficient and stable"
}