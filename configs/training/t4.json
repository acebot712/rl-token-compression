{
  "data_path": "data/processed/processed_data.json",
  "output_dir": "outputs/training_t4",
  "reconstructor_path": "models/reconstructor/fine-tuned/checkpoint-5000",
  "val_data_path": "data/processed/val_data.json",
  "max_epochs": 100,
  "max_steps_per_epoch": 1000,
  "batch_size": 64,
  "learning_rate_policy": 8e-4,
  "learning_rate_reconstructor": 8e-5,
  "context_window": 8,
  "reward_type": "information_theoretic",
  "importance_weighting": false,
  "temperature_schedule": "linear",
  "device": "cuda",
  "save_every": 5,
  "eval_every": 5,
  "patience": 20,
  "log_freq": 50,
  "eval_freq": 500,
  "checkpoint_freq": 500,
  "gradient_accumulation_steps": 16,
  "micro_batch_size": 4,
  "resume": true,
  "use_accelerate": true,
  "mixed_precision": "fp16",
  "gradient_checkpointing": true,
  "deepspeed_config": "configs/deepspeed/zero2.json",
  "comment": "T4-optimized config for NVIDIA T4 GPUs (16GB) - Memory-efficient settings with accelerate, mixed precision, gradient checkpointing, and DeepSpeed ZeRO-2"
}